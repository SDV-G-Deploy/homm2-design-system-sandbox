# HoMM2 Solarpunk v1.1 Pass 03 - Effect Density Hardening

Date: 2026-06-01 12:08-12:11 UTC
Repo: `/root/.openclaw/workspace/homm2-design-system-sandbox`
Ledger: `docs/night-runs/2026-06-01-v1-1-hardening.md`

## Scope

- Read pass 01 and pass 02 artifacts first
- Reduce obvious blur/shadow/gradient heaviness without changing the visual language
- Prefer shared CSS cleanup over page-specific decoration
- Avoid a large CSS rewrite

## Files Changed

- `css/solarpunk.css`

## What Changed

This pass stayed in shared solarpunk styling and did not alter page markup.

Shared proof/system cleanup:

- reduced deep frame shadow weight on `.solar-world-frame` / `.observatory-shell`
- reduced the lower-frame atmospheric overlay opacity on `.solar-world-frame::after`
- reduced glow/drop-shadow intensity and opacity on `.solar-citadel`
- reduced solar disc glow radius/intensity
- reduced hero card and status panel shadow stacks
- reduced the title text-shadow on `.solar-hero-card h1`

Shared solarized applied cleanup:

- reduced brand/nav hover glow intensity in the solarized shell
- reduced interior radial glow in `.solarized-demo .hero-frame`
- reduced moon and gate-arch bloom on the applied shell
- reduced dark panel opacity slightly on shared applied blocks such as `.hero-copy`, `.fairy-hero-copy`, `.operator-card`, and related solarized dark panels
- reduced rail/support background heaviness slightly
- reduced water-glow ring intensity on orbit/ritual accent details
- reduced `.fairy-solarized .fairy-hero-art` opacity and drop-shadow weight
- slightly lightened `.reference-solarized .ref-header` backing opacity

## Why This Stayed Bounded

- no HTML edits
- no component renaming
- no layout changes
- no new assets
- changes were concentrated in the shared solarpunk style layer rather than distributed across each page

## Before / After Evidence

Before captures:

- `tmp/night-runs/2026-06-01-pass-03-before/solarpunk/*`
- `tmp/night-runs/2026-06-01-pass-03-before/index/*`
- `tmp/night-runs/2026-06-01-pass-03-before/fairy/*`
- `tmp/night-runs/2026-06-01-pass-03-before/reference/*`

After captures:

- `tmp/night-runs/2026-06-01-pass-03-after/solarpunk/*`
- `tmp/night-runs/2026-06-01-pass-03-after/index/*`
- `tmp/night-runs/2026-06-01-pass-03-after/fairy/*`
- `tmp/night-runs/2026-06-01-pass-03-after/reference/*`

## Verification Notes

- `solarpunk`, `fairy`, and `reference` after-captures reported zero selector offenders and no document overflow across desktop/mobile/narrow
- `index` still reports one `.hero-frame` internal scrollWidth/clientWidth offender in the visual-capture metrics, but this exact offender already existed in the before-captures and is not introduced by this pass
- visual comparison shows lower bloom and slightly calmer depth without losing the underlying HoMM2 x Solarpunk hierarchy

## Visual Verdict

Verdict: successful narrow hardening pass

Why:

- the proof page remains bright and readable, but the large glows and heavy depth cues are less insistent
- the applied fairy page still feels ceremonial, but the dark stack reads cleaner and less foggy
- the shared solarized shell is slightly less over-processed without erasing the material grammar

## Remaining Risk

- the pass is intentionally subtle; it improves weight, not page-to-page semantic consistency
- `index.html` still sits behind `solarpunk.html` and `fairy-journeys.html` in applied-proof strength
- the persistent `.hero-frame` internal measurement offender on `index` remains a known separate issue outside this pass

## Next Recommendation

Proceed to the reference/applied consistency pass.

Reason:

- the mobile bottleneck is already reduced
- the effect layer is now calmer
- the next useful improvement is aligning what the reference promises with what `solarpunk`, `index`, and `fairy-journeys` actually demonstrate
