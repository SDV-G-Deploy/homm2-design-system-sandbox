# 2026-06-01 v1.1 Closeout

Status: complete

Started: 2026-06-01 13:23 UTC
Completed: 2026-06-01 13:29 UTC

## Scope

- Re-read the v1.1 hardening ledger plus Pass 01, 02, 03, and 05 artifacts and the Pass 04 component contract.
- Re-ran independent verification against the current local/public surfaces.
- Reviewed the result as a closeout critic, not as another implementation pass.

## Final Verdict

Verdict: **portable system, meaningfully hardened, with one applied-page risk and one reference-surface speed debt still open**.

This v1.1 run did not chase a new aesthetic. It made the existing solar-civic HoMM2 direction more trustworthy by tightening mobile budget on the applied page, reducing shared effect heaviness, writing down the reusable contract, and aligning the reference claims with what the real pages actually demonstrate.

The strongest outcome is coherence:

- `solarpunk.html` remains the clearest proof surface.
- `reference.html` now teaches the current system more honestly and lands the component proof earlier.
- `fairy-journeys.html` exposes route/state context sooner on phone widths.
- shared CSS now carries more of the effect and material discipline than before.

## Commit Range

Run base at schedule time:
- `5bba1a7` - `style: compress reference operator surface`

Pass/run commits through closeout start:
- `106cbff` - `docs: schedule v1.1 hardening run`
- `ecc0aba` - `docs: record v1.1 pass 01 evidence`
- `2b3e5c0` - `docs: backfill v1.1 pass 01 commit metadata`
- `4fad49e` - `docs: record pass 01 smoke verification`
- `00d7647` - `style: compress fairy mobile hero`
- `1e62900` - `docs: record pass 02 deploy status`
- `fc6c4a8` - `style: harden solarpunk effect density`
- `fe7625e` - `docs: record pass 03 deploy status`
- `42f3f98` - `docs: add solarpunk component contract`
- `9cf8b5e` - `docs: record pass 04 commit metadata`
- `023d268` - `docs: align reference claims with applied proof`
- `bae8fbe` - `docs: record pass 05 deploy status`

Closeout documentation commit:
- `5dfc3d3` - `docs: add v1.1 closeout report`

## Files Changed In The v1.1 Range

Product/code surfaces:
- `css/fairy-journeys.css`
- `css/solarpunk.css`
- `reference.html`

Docs/artifacts:
- `docs/homm2-solarpunk-component-contract-v1.md`
- `docs/night-runs/2026-06-01-v1-1-hardening.md`
- `docs/night-runs/2026-06-01-v1-1-pass-01-evidence.md`
- `docs/night-runs/2026-06-01-v1-1-pass-02-fairy-mobile.md`
- `docs/night-runs/2026-06-01-v1-1-pass-03-effects.md`
- `docs/night-runs/2026-06-01-v1-1-pass-05-consistency.md`

Range summary:
- `9 files changed, 1326 insertions(+), 58 deletions(-)`

## Independent Verification

### Git / local gates

- `git diff --check` passed before closeout doc edits.
- `node tools/regression-smoke.js` passed.
- Smoke result: `HOMM2 regression smoke OK`; `7 targets` checked across `3 viewports`.

### Closeout captures

Closeout screenshots and metrics were captured under:
- `tmp/night-runs/2026-06-01-pass-06/reference/`
- `tmp/night-runs/2026-06-01-pass-06/solarpunk/`
- `tmp/night-runs/2026-06-01-pass-06/index/`
- `tmp/night-runs/2026-06-01-pass-06/fairy/`

Measured points:

- `reference.html`
  - no document overflow on desktop/mobile/narrow
  - `#component-proof` top: desktop about `305px`, mobile about `251px`, narrow about `249px`
- `solarpunk.html`
  - no document overflow on desktop/mobile/narrow
  - state rows remain visible within the first mobile viewport sequence; first measured state row begins around `698px` on mobile
- `fairy-journeys.html`
  - no document overflow on desktop/mobile/narrow
  - `.fairy-hero-copy` begins around `186px` on mobile/narrow, so the applied page is materially more efficient than Pass 01 but still visually expensive
- `index.html`
  - no document overflow on desktop/mobile/narrow
  - the same pre-existing internal `.hero-frame` capture offender remains in metrics on all three viewports
  - this is consistent with earlier notes and is treated as residual debt, not a new regression from v1.1

### Clipping / state labels

- No horizontal page overflow was detected on the reviewed surfaces.
- No clipped command/state labels were detected in the targeted closeout capture rows.

## Deploy / Live Status

Git state at closeout start:
- local `HEAD`: `bae8fbe`
- local branch: `main`
- upstream: `origin/main`

Live reachability check:
- `reference.html` -> HTTP `200`
- `solarpunk.html` -> HTTP `200`
- `index.html` -> HTTP `200`
- `fairy-journeys.html` -> HTTP `200`

GitHub checks for `bae8fbe`:
- combined commit statuses: none reported
- PR-triggered workflow runs attached to commit: none reported

Interpretation:
- public surfaces are live and reachable
- this repo's Pages deploy remains operational from the user perspective
- GitHub did not expose a commit-linked workflow record/status for the reviewed closeout head

## Remaining Risks

### 1. P1 - `index.html` still carries a persistent internal `.hero-frame` overflow offender in capture metrics

This is not page-level overflow and did not regress in this run, but it is the clearest remaining measurable layout debt on the public front door.

Best fix:
- run one bounded `index.html` internal-width hardening pass focused on the hero frame/container contract, not a broad redesign.

### 2. P1 - `fairy-journeys.html` mobile hero is improved but still tall for a route-first applied page

Pass 02 solved the worst first-screen compression issue, but the closeout capture still shows a large ceremonial hero object before more downstream route/state material.

Best fix:
- do one more narrow applied pass only if the goal is stronger route-first mobile utility rather than atmosphere preservation.

### 3. P2 - `reference.html` is now honest and earlier, but still not maximally fast as an operator/manual front door

The canonical proof now arrives meaningfully earlier than the old manual-first version, but a future operator-surface compression pass could still make the component proof feel even more immediate on desktop.

Best fix:
- optional bounded compression pass after any `index` contract fix, not before.

## Best Next Step

Best next step: **bounded `index.html` hero-frame contract pass**.

Reason:
- it is the clearest remaining measurable defect;
- it affects the public entry surface;
- it is narrower and less subjective than another atmosphere pass.

If no more implementation is planned immediately, the v1.1 run is already a valid stopping point. The direction is coherent, the proof is portable, and the remaining debt is now specific rather than fuzzy.

## Russian Closeout Report

Утренний итог по v1.1:

- Серия закрыта без блокеров.
- Главные продуктовые коммиты:
  - `00d7647` - ужат mobile hero на `fairy-journeys.html`
  - `fc6c4a8` - снижена общая effect density в shared CSS
  - `42f3f98` - зафиксирован переносимый component/material/state contract
  - `023d268` - reference приведён в соответствие с реально показанным applied proof
- Финальная независимая проверка зелёная:
  - `git diff --check` passed
  - `node tools/regression-smoke.js` passed
  - closeout-captures сохранены в `tmp/night-runs/2026-06-01-pass-06/`
  - public `reference`, `solarpunk`, `index`, `fairy-journeys` отвечают `HTTP 200`
- Что осталось:
  - у `index.html` сохраняется старый внутренний `.hero-frame` width offender в capture-метриках
  - `fairy-journeys.html` на mobile стал лучше, но hero всё ещё высокий
  - `reference.html` уже честнее и быстрее, но его ещё можно сжать как operator/manual surface
- Лучший следующий шаг:
  - один узкий pass на `index.html` для hero-frame/container contract, без нового редизайна.
